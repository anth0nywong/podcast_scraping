<!DOCTYPE html>
<html>
  <head>
    <title>Scraping App</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <h1><%=title%></h1>
    <p>Welcome to my scrapping app, which scrap the podcasts from software engineering daily.</p>
    <% for (var i = 0; i < podcasts.length; i ++) { %>
      <h2><%=podcasts[i].title%></h2>
      <div>
      <audio controls>
        <source type="audio/mpeg" src="<%=podcasts[i].link%>">
        <a href="<%=podcasts[i].link%>"><%=podcasts[i].link%></a>
      </audio>
      </div>
      <% }; %>
      <% if(parseInt(window.location.pathname.split('/')[2] > 1) { %> window<button type="button" onclick="prevPage()">Previous Page</button> <% } ^>
      <button type="button" onclick="nextPage()">Next Page</button>
  </body>
</html>

<script>

function prevPage() {
    const page = parseInt(window.location.pathname.split('/')[2]) - 1;
    window.location = `/page/${page}`;
  }
  function nextPage() {
    const page = parseInt(window.location.pathname.split('/')[2]) + 1;
    window.location = `/page/${page}`;
  }
  </script>